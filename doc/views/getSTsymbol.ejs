<%- include header %>

<div class="app-body">
  <%- include menu %>
  <main class="main">
    <!-- Breadcrumb-->
    <ol class="breadcrumb">
      <li class="breadcrumb-item">ST合约</li>
      <li class="breadcrumb-item active">获取ST的符号</li>
    </ol>

    <div class="container-fluid">
      <div class="animated fadeIn">
        <div class="card">
          <div class="card-header">getSTsymbol(securityToken)</div>
          <div class="card-body">
            <div class="bd-example">
              获取ST的符号
          </div>
            </div>
        </div>
        </div>
    </div>

    <%- include code %>

  </main>
</div>
<script type="text/javascript">

  function runCode() {
    getSTsymbol(config.contract.ST).then();
  }

  async function getSTsymbol(securityToken) {
    try {
      var contract = {address: securityToken, abi: abiSecurityToken};
      var st = new SecurityToken(web3, contract);
      var res  = await st.symbol();
      res = web3.toAscii(res);
      var result = "result:" + res + "\n";
    } catch (e) {
      var result = "error：" + e + "\n";
    }

    setResult(result);

  }

  // var Web3 = require('web3');
  $(document).ready(function() {
    var code = runCode.toString()+"\n\n";
    code += getSTsymbol.toString() + "\n\n";
    code += "runCode();\n\n";
    setCode(code);

  });

</script>
<%- include footer %>
